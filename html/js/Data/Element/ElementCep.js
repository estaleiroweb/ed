;(function ($, window, document, undefined) {
	function ElementCep(element, options) {
		$.Element.call(this,element, options); // call super constructor.
	}
	ElementCep.extends($.Element).jQuery();
})(jQuery, window, document);
