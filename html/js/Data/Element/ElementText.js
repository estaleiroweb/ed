;(function ($, window, document, undefined) {
	function ElementText(element, options) {
		$.Element.call(this,element, options); // call super constructor.
	}
	ElementText.extends($.Element).jQuery();
})(jQuery, window, document);
